# Feedback App - React

Features of this application include:

- Real time validation: users cannot submit a review until the string length is greater than or equal to 10. Once the string length reaches 10, the button is enabled.
